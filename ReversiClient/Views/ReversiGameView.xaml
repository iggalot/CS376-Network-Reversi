<UserControl x:Class="ReversiClient.Views.ReversiGameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReversiClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="Gray" Margin="5" Width="500">
        <StackPanel Orientation="Vertical">
            <Rectangle Margin="15" Fill="Green" Height="200" Width="200"/>
            <Label Content="{Binding TestText}" Width="200" Height="50" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" Background="Blue"/>

            <Label Content="{Binding GameboardViewModel.GameboardViewModelText}" Width="200" Height="50" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" Background="Blue"/>
            <Canvas Name="GameArea" ClipToBounds="True" Width="300" Height="300">
                <local:ReversiGameboardView DataContext="{Binding GameboardViewModel}"/>
            </Canvas>

            <Label Content="After Canvas" FontSize="30"/>
        </StackPanel>

    </Grid>
</UserControl>
